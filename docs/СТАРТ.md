# 🚀 ЗАПУСК PLUELY С РУССКИМ ПЕРЕВОДОМ

## ✅ ЧТО СДЕЛАНО

Полностью реализована система i18n:
- ✅ **150+ ключей** переведены на русский
- ✅ **Компонент выбора языка** с иконкой 🌐
- ✅ **Автоопределение** языка браузера  
- ✅ **Сохранение** выбора в localStorage
- ✅ **TypeScript** полная типизация
- ✅ **3 компонента** уже переведены (примеры)

## 🎯 КАК ЗАПУСТИТЬ

### Проблема
VS Code из snap создаёт конфликт библиотек. Приложение НЕ запустится из терминала VS Code.

### ✅ РЕШЕНИЕ: Откройте СИСТЕМНЫЙ терминал

```bash
# 1. Откройте терминал (Ctrl+Alt+T)
cd /home/vodorod/dorod/pluely_ru

# 2. Запустите Vite (в фоне)
npm run dev &

# 3. Подождите 5 секунд
sleep 5

# 4. Запустите приложение
./src-tauri/target/debug/pluely
```

## 🔍 КАК ПРОВЕРИТЬ

После запуска:

1. **ОТКРОЕТСЯ ОТДЕЛЬНОЕ ОКНО** (не браузер!)
2. Найдите иконку **🌐** в интерфейсе (обычно справа вверху или в настройках)
3. Кликните → выберите **Русский**  
4. Интерфейс должен смениться на русский язык
5. Закройте и откройте → язык сохранится!

## 📂 СТРУКТУРА ФАЙЛОВ

```
src/
├── locales/
│   ├── en.json          # Английский (150+ ключей)
│   └── ru.json          # Русский (150+ ключей) ✨
├── contexts/
│   ├── I18nContext.tsx  # React Context
│   └── index.ts         # Экспорт
├── components/
│   ├── LanguageSelector.tsx  # Компонент выбора языка 🌐
│   └── index.ts         # Экспорт
├── types/
│   └── i18n.ts          # TypeScript типы
└── main.tsx             # I18nProvider интегрирован ✅
```

## 📝 ИСПОЛЬЗОВАНИЕ В КОДЕ

```tsx
import { useTranslation } from './contexts';

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <div>
      <h1>{t("app.name")}</h1>
      <p>{t("app.description")}</p>
    </div>
  );
}
```

## 🌍 ДОБАВЛЕНИЕ НОВЫХ ЯЗЫКОВ

1. Создайте файл `src/locales/[код].json`
2. Скопируйте структуру из `en.json`
3. Переведите все ключи
4. Добавьте в `src/types/i18n.ts`:
   ```ts
   export type Locale = 'en' | 'ru' | '[код]';
   ```
5. Добавьте в `LanguageSelector.tsx`

## 📚 ДОКУМЕНТАЦИЯ

Создано 7 файлов документации (бэкап):
- `I18N_GUIDE.md` - Полное руководство
- `MIGRATION_GUIDE.md` - Миграция компонентов
- `LANGUAGES_ROADMAP.md` - Шаблоны для 10+ языков
- `README_RU.md` - Русское README
- `I18N_CHEATSHEET.md` - Шпаргалка
- `IMPLEMENTATION_REPORT.md` - Технический отчёт
- `PUBLISH_INSTRUCTIONS.md` - Публикация на GitHub

Все файлы в: `/home/vodorod/dorod/pluely_ru_backup/`

## ⚠️ ВАЖНО

- Это контрольная работа - всё должно работать!
- Приложение - DESKTOP, не браузер!
- Запускать из СИСТЕМНОГО терминала, не из VS Code
- Остальные 47 компонентов нужно мигрировать по примеру

## 🎓 УСПЕХОВ С КОНТРОЛЬНОЙ!

После проверки работы переводов можно:
1. Мигрировать остальные компоненты
2. Добавить новые языки
3. Опубликовать на GitHub
