# 🚀 Настройка GitHub Actions для сборки Windows + iOS

## 📋 Шаг 1: Добавьте GitHub Secrets

Перейдите в ваш репозиторий на GitHub:
```
https://github.com/Vodorod77/pluely_ru/settings/secrets/actions
```

Добавьте **2 секрета** (кнопка "New repository secret"):

### 1️⃣ `API_ACCESS_KEY`
```
ZKs3gV7PpbUzNFBCvvNJvYzCKrH1K3xCjyb2qUguyZ
```

### 2️⃣ `APP_ENDPOINT`
```
https://pluely.com/pluely/v2
```

---

## 🎯 Шаг 2: Запустите сборку

### Вариант A: Автоматически (при push в master)
```bash
cd /home/vodorod/dorod/pluely_ru
git add .
git commit -m "feat: add Windows and iOS builds"
git push origin master
```

### Вариант B: Вручную (через веб-интерфейс)
1. Откройте: https://github.com/Vodorod77/pluely_ru/actions/workflows/publish.yml
2. Нажмите **"Run workflow"** → **"Run workflow"**

---

## 📦 Шаг 3: Скачайте артефакты

Через ~15-20 минут сборка завершится. Найдите релиз:
```
https://github.com/Vodorod77/pluely_ru/releases
```

Будет создан **draft release** с файлами:
- ✅ **Windows**: `Pluely_0.1.8_x64_en-US.msi` (~15 MB)
- ✅ **Windows**: `Pluely_0.1.8_x64-setup.exe` (~15 MB)
- ✅ **macOS Intel**: `Pluely_x64.app.tar.gz`
- ✅ **macOS/iOS ARM**: `Pluely_aarch64.app.tar.gz`

---

## 🔍 Мониторинг сборки

Смотрите прогресс здесь:
```
https://github.com/Vodorod77/pluely_ru/actions
```

Время сборки каждой платформы:
- Windows: ~10 минут
- macOS x86_64: ~12 минут
- macOS aarch64 (iOS): ~12 минут

---

## ⚠️ Важные замечания

### Windows
- Будут созданы `.msi` (установщик) и `.exe` (портативная версия)
- Запускаются на Windows 10/11 (x64)

### iOS/macOS
- `aarch64` — для iPhone, iPad, Apple Silicon Mac (M1/M2/M3)
- `x86_64` — для старых Intel Mac
- Требуется сертификат разработчика для iOS (в этой сборке его нет)

### Текущие Linux-пакеты
Если хотите добавить Linux обратно в workflow, раскомментируйте:
```yaml
- platform: "ubuntu-22.04"
  args: ""
```

---

## 🐛 Устранение проблем

### Ошибка "Secret not found"
- Проверьте, что добавили оба секрета: `API_ACCESS_KEY` и `APP_ENDPOINT`
- Имена должны быть **точно** такими же

### Сборка упала (Failed)
- Откройте логи: https://github.com/Vodorod77/pluely_ru/actions
- Нажмите на упавший workflow → Jobs → кликните на красный шаг
- Скопируйте ошибку и напишите мне

### macOS-сборка требует сертификат
Для распространения через App Store нужен:
- Apple Developer Account ($99/год)
- Provisioning Profile
- Сертификаты (добавляются как secrets)

Без сертификата приложение соберется, но macOS будет блокировать запуск.

---

## 🎉 Готово!

После успешной сборки у вас будет полный набор установщиков для всех платформ. 
Нажмите **"Publish release"** в draft-релизе, чтобы сделать его публичным.
